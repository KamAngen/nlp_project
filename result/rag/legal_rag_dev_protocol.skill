# Legal RAG Development & Maintenance Skill (v1.0)
# 严格执行规约 - 严禁违背

## [核心禁令]
1. **禁全量重写**：禁止在未完全理解原有函数业务逻辑的情况下，使用简化版逻辑替换功能完善的既有代码。
2. **禁冗余残留**：每次编辑完前端脚本后，必须全局检索是否存在同名函数覆盖问题。
3. **禁无序追加**：严禁在持久化层（JSONL）使用 append 模式进行无去重校验的批量插入。

## [强制执行流]
### 第一阶段：编辑前置审计
- 必须使用 `view_file` 完整阅读目标函数及其上下游关联逻辑（至少查看函数前后 50 行）。
- 确认原有功能清单（如：进度条、ID去重、分片处理、错误回显）。

### 第二阶段：手术式精准编辑
- 仅修改必要的代码行，严禁为了“代码整洁”而改变原本正确的变量命名或缩进风格。
- 涉及数据持久化时，必须优先确保 Upsert（存在即更新）逻辑，并采用全量写回模式防止数据膨胀。

### 第三阶段：功能回归校验
- 修改 UI 布局后，必须核对 HTML 元素的 ID 是否改变、事件绑定是否失效。
- 确认后端接口返回格式与前端 `fetch` 解析逻辑是否依然对齐。
- 检查是否存在代码截断（如：漏掉闭合括号或分号）。

## [异常处理]
- 如果发现系统数据出现异常（如：重复条目），必须先强行停止服务，执行 `data_cleanup.py` 脚本，再重新启动。
- 任何会导致全量重建索引的操作，必须提前向用户说明原因并获得准许。
